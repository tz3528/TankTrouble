### 2025年3月28日

定时任务管理器的设计思路

- 定义一个任务结构用以存储文件描述符、回调函数、下一次执行的时间。
- 用set加哈希表的方式实现快速存取，以及获得最早需要执行的任务。
- 添加任务的函数中，参数传入距离下一次执行的时间，而回调函数的返回值也表示距离下一次执行的时间，如果时间为0，表示不再重复该任务，这样可以方便的在函数里面根据各种逻辑来得出可变的定时任务。
- 定时器的基本时钟单元是1毫秒，所有的定时任务的定时都必须是1毫秒的正整数倍。

对坦克池、子弹池的状态更新已改为定时任务，由定时任务管理器完成。

### 2025年3月31日

完成了客户端的心跳发送以及服务器的心跳接收。

对于CtoI函数，进行了修改，进行了类型的强制转换，因为char类型是整型，应当变为非负整型。

```c++
int sum=(unsigned char)str[0]|((unsigned char)str[1]<<8)|((unsigned char)str[2]<<16)|((unsigned char)str[3]<<24);
```

Contanier容器的toString，对于基本类型，改用memcpy，相比原先会安全一些，至少不需要考虑会不会变成负数的情形，只需要考虑每个字节的转换即可。

另外，对于使用memcpy，一定要先对其申请一定大小的内存才能够用

```c++
string value(sizeof(T), '\0');
memcpy(value.data(), &t, sizeof(T));
```

对于服务器中的监听事件，

```c++
if(events[i].data.fd == listen_fd)
```

这个分支内events[i].data.fd表示监听事件的文件描述符，而client_fd才是被监听事件的文件描述符，原先的代码存在误用

### 2025年4月8日

关于监测器的设计。主要目的是分析某段代码的运行效率，然后为了方便事后分析，输出成excel格式，再用其它可视化的方法展现结果。

对于要监测的内容，有下面几个困难需要解决：

- 首先不同线程间是隔离的，即不同线程调用同一段代码块，时间戳应是互不影响的。同一个线程的时间戳应当是相互嵌套的，而非相交的。
- 其次，需要获取当前代码段落所属的函数、行号、文件路径，方便定位，但是如果是手动输入，就会略显繁琐。
- 另外，两类任务中，对于固定时间间隔监测执行次数，应当有一个定时器来输出。

针对上述困难做了如下的设计

- 对于需要线程间隔离的资源，利用关键字thread_local，由系统实现。
- 对于第二点，利用c++20中的source_location即可获取，兼容低版本可以考虑宏__FILE__.
- 对于第三点，添加一个私有计时器即可。

这期间顺手解决了帧率控制问题。

### 2025年4月9日

绘图效率进行了一定改善，经检测

```c++
FillRect(hdcMem, &rect, WhiteBrush);
```

上面的代码占据了绘图的一半时间左右约900到1000微秒。注意到每一次都是填充为纯白色的背景，因此可以预处理一个纯白背景的位图，然后每次都先换上这个位图，然后再在缓存的位图上绘制。

经优化，绘制白色背景大约需要280微秒。总的绘画时间大约要1300微妙。占用时间约为原先的三分之二。

另外deepseek提供了一种方案，就是每次只更新被修改的区域，但仍然使用填充的方式，这对于小范围的修改明显是更优的，但是为了有更稳定的运行时间，不采纳这个方案，这是因为墙壁重新绘制时，已经覆盖了几乎所有区域。

关于帧率，经检测，目前250帧是稳定的，500帧是较不平稳的的，平均450帧，但是定到1000帧，则绘图次数明显小于绘图消息数。

### 2025年4月9日

对于锁的使用进行了一定的优化。

经验证

```c++
for (auto& Tank : TankPool) {
	Tank->draw(hdcMem);
}
```

平均执行时间为80us，而仅进行如下复制的话在10us多一些，

```c++
list<shared_ptr<Tank>> tanksCopy;
 {
	shared_lock<shared_mutex> lock(tpMutex);monitor.BeginPerRun();
	tanksCopy = TankPool;
	monitor.EndPerRun();
}
```

这说明通过复制能够明显减少对锁的占用时间。另一方面，如下所示的做法

```C++
monitor.BeginPerRun();
list<shared_ptr<Tank>> tanksCopy;
{
	shared_lock<shared_mutex> lock(tpMutex);
	tanksCopy = TankPool;
	
}
 
for (auto& Tank : TankPool) {
Tank->draw(hdcMem);
}
monitor.EndPerRun();
```

总耗时平均108us，在对绘图效率损耗不大的情形下，明显降低了对锁的占用时间，故在绘图中改用下面的做法。

对于如下锁征用

```c++
monitor.BeginPerRun();
shared_lock<shared_mutex> lock(tpMutex);
monitor.EndPerRun();
```

平均抢占时间是16us，而将所有的互斥锁更改为了读写锁之后，锁的平均征用时间是9us。另外，使用如下方式的锁征用平均为6us

```c++
std::lock_guard<std::mutex > lock(tpMutex);
```

效率上讲区别，锁征用并非效率瓶颈，但是互斥锁不光在锁征用上效率高，也更便于开发，因此将所有读写锁改写回互斥锁，并使用lock_guard自动管理其生命周期。
