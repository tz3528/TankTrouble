### 2025年3月28日

定时任务管理器的设计思路

- 定义一个任务结构用以存储文件描述符、回调函数、下一次执行的时间。
- 用set加哈希表的方式实现快速存取，以及获得最早需要执行的任务。
- 添加任务的函数中，参数传入距离下一次执行的时间，而回调函数的返回值也表示距离下一次执行的时间，如果时间为0，表示不再重复该任务，这样可以方便的在函数里面根据各种逻辑来得出可变的定时任务。
- 定时器的基本时钟单元是1毫秒，所有的定时任务的定时都必须是1毫秒的正整数倍。

对坦克池、子弹池的状态更新已改为定时任务，由定时任务管理器完成。

### 2025年3月31日

完成了客户端的心跳发送以及服务器的心跳接收。

对于CtoI函数，进行了修改，进行了类型的强制转换，因为char类型是整型，应当变为非负整型。

```c++
int sum=(unsigned char)str[0]|((unsigned char)str[1]<<8)|((unsigned char)str[2]<<16)|((unsigned char)str[3]<<24);
```

Contanier容器的toString，对于基本类型，改用memcpy，相比原先会安全一些，至少不需要考虑会不会变成负数的情形，只需要考虑每个字节的转换即可。

另外，对于使用memcpy，一定要先对其申请一定大小的内存才能够用

```c++
string value(sizeof(T), '\0');
memcpy(value.data(), &t, sizeof(T));
```

对于服务器中的监听事件，

```c++
if(events[i].data.fd == listen_fd)
```

这个分支内events[i].data.fd表示监听事件的文件描述符，而client_fd才是被监听事件的文件描述符，原先的代码存在误用

### 2025年4月8日

关于监测器的设计。主要目的是分析某段代码的运行效率，然后为了方便事后分析，输出成excel格式，再用其它可视化的方法展现结果。

对于要监测的内容，有下面几个困难需要解决：

- 首先不同线程间是隔离的，即不同线程调用同一段代码块，时间戳应是互不影响的。同一个线程的时间戳应当是相互嵌套的，而非相交的。
- 其次，需要获取当前代码段落所属的函数、行号、文件路径，方便定位，但是如果是手动输入，就会略显繁琐。
- 另外，两类任务中，对于固定时间间隔监测执行次数，应当有一个定时器来输出。

针对上述困难做了如下的设计

- 对于需要线程间隔离的资源，利用关键字thread_local，由系统实现。
- 对于第二点，利用c++20中的source_location即可获取，兼容低版本可以考虑宏__FILE__.
- 对于第三点，添加一个私有计时器即可。

这期间顺手解决了帧率控制问题。
