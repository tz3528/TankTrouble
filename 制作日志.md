### 2025年2月11日

更新了Tank.cpp中后退过程左右转的逻辑。

学习了手动提交git更新代码。

尝试了direct2d，但是失败了。

GDI+也失败了。

计划从明天开始，做开始游戏界面的ui，以及联网部分的学习。

### 2025年2月12日

计划在虚拟机建立服务器。

添加了坦克的攻击间隔，增加坦克的子弹数为20。

制作了开始页面的ui，以及分离了回调函数。

### 2025年2月13日

确定了用虚拟机实现服务器的可行性。

### 2025年2月14日

服务器计划用cmake，而不是vs里的解决方案。

### 2025年2月15日

今天爆改ui的控制，最大的难题就是button控件初始化时候赋的id和点击时消息发出的id不同。

发现的问题就是，在HMENU部分，如果直接调用宏定义，例如

```c++
(HMENU)SINGLE
```

由于宏定义是一块内存，把整个表达式都转换了，所以实际的类型转换是

```c++
(HMENU)BUTTON_BEGIN + 1
```

这个东西只转换了前面的部分，而后面的部分会被乘4处理，所以导致了按钮点击的时候id和定于时不同。

正确的用法应该是下面的形式

```c++
(HMENU)(SINGLE)
```

这算是养成了个好习惯，用宏定义时加个括号



### 2025年2月16日

将控件部分分离出来，并封装了win32控件中单选按钮组。

计划两条路一起走，一边是完善ui，然后制作地图的随机生成规则，以及人机的对战逻辑。

另一方面是联网对战，先学会通信的原理，然后制作联网对战的房间ui，以及规定服务器和客户端各自需要负责的逻辑。

粗略来看第一部分需要10天，第二部分需要14天。

至于说一些细节再慢慢完善。

### 2025年2月17日

完善了ui，做了3组单选框，并用变量存下了所选的内容。

下一阶段开始做地图的生成算。

服务器的项目建了一个cmake项目。

### 2025年2月18日

地图生成大致有了方向，每张地图有几块区域设置为关键点，然后从关键点开始dfs。

设置关键点是为了尽可能保证每一块关键点是联通的，即地图上会有相对空旷的区域用来打架。

然后粗算了一下概率，dfs过程中，每条边有$\frac{1}{3}$的概率被打通，这样算下来会有$\frac{5}{9}$的边被移除。

函数写了一点点，明天应该可以实现完，如果效果不错那这一部分就完成了。

服务器尝试远程连接，能够连到linux端，但是生成的文件没有传输。

### 2025年2月19日

换了搜索方式，依然是dfs，但是一直re，没有调出来。

### 2025年2月20日

今天战果丰富。

首先完成了地图的随机生成。然后调整了坦克和子弹的移动速度，手感更好了。

其次将单机游戏的初始化和消息回调函数单独放在了一个文件中，这样窗口文件就更小了。

服务器方面成功用vs连接到了linux虚拟机，并且能够生成可执行文件，能够成功运行。

计划明天完成出生点位和出生朝向的随机生成，以及初步设计人机操作的基本模式。

再就是服务器方面应当能够正常收发。

### 2025年2月21日

今天就只对代码修修补补，改了文件格式。

人机计划有三个事件，分别时寻径、攻击和躲避。然后事件之间轮流判断，每个事件中间隔5毫秒。

服务器及客户端的通信在明天开始写。

### 2025年2月22日

今天树立了AI的思路。移动方面，通过加权子弹轨迹有向线段到坦克的距离，以及寻径距离，求加权和的方向，来判断前进、后退，以及左转、右转。

攻击的话，判断寻径3格内是否有玩家，如果有玩家且与自己朝向夹角在45度内，就攻击。

网络编程部分接着看了视频，不过大致思路是有的，就是发socket。主要难点是服务器和客户端分别处理什么内容。

两天时间应该能够解决ai的设计。明天实现网络通信。

再之后，设计联网对战的ui，对战房间的设计。

这样想来第一部分算是能按时完成，第二部分能够提前完成。

### 2025年2月23日

做了一下移动的模式，但是还没测试，攻击没有写。

服务端绑定不了端口，很奇怪。

### 2025年2月24日

ai部分算是写好了，但是操控ai的线程池还在学。

网络通信这边被头文件整惨了，加了一句话整好了，但是还不能通信。

### 2025年2月25日

网络部分一点没动，光是一个多线程就给我人整麻了，好在线程池写好了，而且没有问题。

现在碰到一个问题，改用共享锁后，开始有屏闪。

### 2025年2月26日

总算是解决了上面的出现过的所有问题。

ai部分的逻辑还存在问题

### 2025年2月27日

今天解决了一个出现了很久的问题，就是当攻击出的子弹变得很多的时候就会卡住，然后发现产生了死锁，是锁的争用问题。

经研究发现，坦克只会往子弹池里加子弹，在提供了子弹构造函数的参数之后就不会影响子弹的参数，所以关于子弹池的锁是没有必要的，因为不会有脏读等情况发生，而子弹池枚举子弹是按顺序进行的，所以被添加到末尾的子弹会在一个巡回的最后枚举到，即使一个周期结束，恰好跳过了它，那么下一周期也会在很短时间内开始，其对坦克的影响不大。
